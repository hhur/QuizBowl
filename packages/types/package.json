{
  "name": "@quizbowlhub/types",
  "version": "0.1.0",
  "description": "Shared TypeScript types for QuizBowlHub",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist/**/*"
  ],
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@quizbowlhub/config": "*",
    "typescript": "^5.2.2"
  }
}